{
  "name": "promptprotect",
  "version": "0.1.0",
  "private": true,
  "description": "Chrome extension that blocks accidental secret and PII leaks to web-based LLMs.",
  "scripts": {
    "build": "node build.mjs",
    "dev": "node build.mjs --watch",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.38",
    "esbuild": "^0.27.4",
    "typescript": "^6.0.2"
  }
}

